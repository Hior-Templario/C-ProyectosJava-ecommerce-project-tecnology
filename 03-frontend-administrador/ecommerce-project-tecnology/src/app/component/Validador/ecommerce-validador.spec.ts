import { EcommerceValidador } from './ecommerce-validador';

describe('EcommerceValidador', () => {
  it('should create an instance', () => {
    expect(new EcommerceValidador()).toBeTruthy();
  });
});
